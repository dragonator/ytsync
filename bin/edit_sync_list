#!/usr/bin/env python
import os

script_abs_path = os.path.dirname(os.path.abspath(__file__))

if __name__ == '__main__':
    command = 'python {}'\
        .format(os.sep.join([script_abs_path, 'src', 'edit_sync_list.py']))
    os.system(command)
